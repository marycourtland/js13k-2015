
// HTML ==============================================================
var wnd = window,
    doc = document, 
    $ = function () { return doc.querySelector.apply(doc, arguments); }
;


// Math ==============================================================
var xy = function(x, y) { return {x:x, y:y}; },
    rnd = Math.random
;


// Math ==============================================================